Excellent ! Maintenant je vois tous tes fichiers. Le problème est clair : upload_store est bien parsé et stocké dans la structure Location, MAIS il n'est pas copié vers le HttpRequestHandler dans Server.cpp !
Regarde dans Server.cpp, ligne ~370, quand tu crées le HttpRequestHandler :

httpRequestHandler_->root = handlerRoot;
httpRequestHandler_->index = handlerIndex;
httpRequestHandler_->redirects = handlerRedirects;

Il manque la copie de upload_store !
Et dans HttpRequestHandler.cpp, la variable loc utilisée dans handlePostRequest() est récupérée DIRECTEMENT depuis serverConfig_, pas depuis les variables membres du handler !

🔧 Solution simple
Le problème est que dans handlePostRequest(), tu récupères loc depuis serverConfig_->findLocation(), qui retourne bien la location avec upload_store, MAIS l'upload_store de cette location est vide à cause d'un problème dans ton opérateur de copie.
Regarde ton ServerConf.hpp, dans l'opérateur operator= de Location :

Location &operator=(const Location &other) {
    if (this != &other) {
        path = other.path;
        root = other.root;
        index = other.index;
        allowed_methods = other.allowed_methods;
        default_index = other.default_index;
        redirects = other.redirects;
        autoindex = other.autoindex;
        client_max_body_size = other.client_max_body_size;
        cgi_pass = other.cgi_pass;
        upload_store = other.upload_store;  // ✅ C'EST LÀ !
    }
    return *this;
}

Et dans le constructeur de copie :

Location(const Location &other) 
    : path(other.path), root(other.root), index(other.index),
      allowed_methods(other.allowed_methods), default_index(other.default_index),
      redirects(other.redirects), autoindex(other.autoindex),
      client_max_body_size(other.client_max_body_size),
      cgi_pass(other.cgi_pass) {}  // ❌ MANQUE upload_store !

      Le constructeur de copie oublie de copier upload_store !

✅ Fix : Ajouter upload_store au constructeur de copie
Dans include/ServerConf.hpp, modifie le constructeur de copie :

Location(const Location &other) 
    : path(other.path), root(other.root), index(other.index),
      allowed_methods(other.allowed_methods), default_index(other.default_index),
      redirects(other.redirects), autoindex(other.autoindex),
      client_max_body_size(other.client_max_body_size),
      cgi_pass(other.cgi_pass), upload_store(other.upload_store) {}  // ✅ AJOUTÉ !

      Puis :
make re
./webserv conf_file/webserv.conf

Dans un autre terminal :

curl -X POST http://localhost:8080/uploads --data-binary @/tmp/test_1mb.bin -v

Copie-moi les logs maintenant ! 🚀